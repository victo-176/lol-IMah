# Telegram Bot Framework (async, production-grade)
aiogram==3.15.0
aiohttp>=3.9.0,<4.0.0

# WebSocket (raw aiohttp handles Socket.IO v4 framing)
# python-socketio/engineio removed — avoids ClientWSTimeout and auth issues

# Redis (async state store + caching)
redis[hiredis]>=5.0.0,<6.0.0

# Database ORM
sqlalchemy[asyncio]>=2.0.0,<3.0.0
aiosqlite>=0.19.0,<1.0.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
urllib3>=2.0.0,<3.0.0
structlog>=24.0.0,<25.0.0
